
//components
import Profile from "../profile";

import user from '../../API/user.json'

const App: React.FC = () => {
  return (
    <>
      <Profile
        name={user.name}
        tag={user.tag}
        location={user.location}
        avatar={user.avatar}
        stats={user.stats}
      />
    </>
  );
}

export default App;
